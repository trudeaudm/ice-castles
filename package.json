{
  "name": "icecastles-guest-app",
  "version": "0.1.0",
  "private": true,
  "description": "Mobile-first guest map, discovery and scavenger-hunt web app for Ice Castles",
  "type": "commonjs",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "seed": "node server/seed.js",
    "reset": "node server/seed.js --force"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "express": "^4.21.2",
    "pg": "^8.13.1",
    "qrcode": "^1.5.4"
  }
}
